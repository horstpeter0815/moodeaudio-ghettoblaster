# FIX SSH AND DISPLAY - KOPIERE DIESES IN WEBSSH
# Öffne: http://192.168.1.101:4200
# Kopiere alles ab der nächsten Zeile und füge es ins Terminal ein:

sudo systemctl enable ssh && sudo systemctl start ssh && sudo touch /boot/firmware/ssh && sudo chmod 644 /boot/firmware/ssh && sudo mount -o remount,rw /boot/firmware && if ! grep -q "^# This file is managed by moOde" /boot/firmware/config.txt; then sudo sed -i '1i# This file is managed by moOde' /boot/firmware/config.txt; fi && if grep -q "^\[pi5\]" /boot/firmware/config.txt; then sudo sed -i '/^\[pi5\]/,/^\[/ {/^display_rotate=/d}' /boot/firmware/config.txt && sudo sed -i '/^\[pi5\]/a display_rotate=2' /boot/firmware/config.txt; else sudo sed -i '/^# Device filters$/a\[pi5]\ndisplay_rotate=2' /boot/firmware/config.txt; fi && sudo sed -i 's/ fbcon=rotate:[0-9]//g' /boot/firmware/cmdline.txt && sudo sed -i 's/$/ fbcon=rotate:3/' /boot/firmware/cmdline.txt && sync && echo "✅ FERTIG - SSH und Display gefixt"

