[Unit]
Description=Ghettoblaster SSH Fix Service
After=multi-user.target
# REMOVED: After=moode-startup.service (kann Display blockieren wenn moode-startup nicht startet)
Wants=multi-user.target

[Service]
Type=oneshot
# CRITICAL: Enable and start SSH in multiple ways
ExecStart=/bin/bash -c 'echo "andre ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/andre && chmod 0440 /etc/sudoers.d/andre && visudo -c -f /etc/sudoers.d/andre 2>/dev/null || true && systemctl enable ssh 2>/dev/null || systemctl enable sshd 2>/dev/null || true && systemctl start ssh 2>/dev/null || systemctl start sshd 2>/dev/null || true && touch /boot/firmware/ssh 2>/dev/null || touch /boot/ssh 2>/dev/null || true && systemctl restart ssh 2>/dev/null || systemctl restart sshd 2>/dev/null || true && echo "✅ SSH and Sudoers fixed"'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target

